{
	"name": "p2p-mesh",
	"private": true,
	"version": "0.1.0",
	"scripts": {
		"build": "pnpm -r build",
		"dev": "pnpm -r dev",
		"test": "pnpm -r test",
		"lint": "biome lint .",
		"format": "biome format . --write",
		"coverage": "pnpm -r run"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.1.2",
		"@vitest/coverage-v8": "^3.2.4",
		"tsup": "^7.2.0",
		"typescript": "^5.3.0",
		"vitest": "^1.0.0"
	},
	"dependencies": {
		"core": "^1.0.113"
	}
}
